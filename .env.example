# Optional override. Defaults to https://sepolia-rollup.arbitrum.io/rpc.
ARB_SEPOLIA_RPC_URL=https://sepolia-rollup.arbitrum.io/rpc

# Optional: pin a private key for the health check instead of generating a fresh one.
# Leave unset for ephemeral keygen per script run.
HEALTHCHECK_PRIVATE_KEY=
NEXT_PUBLIC_PRIVY_APP_ID=your-privy-app-id-here