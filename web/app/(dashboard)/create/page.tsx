import {PagePlaceholder} from "../_PagePlaceholder";

export default function CreatePage(): React.ReactElement {
  return (
    <PagePlaceholder
      num="F11"
      title="Create market."
      emphasis="With ChainGPT."
      meta="04 / 04"
      comingIn="PHASE F11"
      description="Conversational market creation through ChainGPT. Natural-language question → resolution criteria → oracle wiring → Safe-cosigned createMarket. Preview before submission."
    />
  );
}
